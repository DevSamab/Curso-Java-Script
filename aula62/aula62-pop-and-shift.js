const nomes = ['Eduardo', 'Maria', 'Joana'];
const removido = nomes.pop(); // Essa linha usa o método pop para remover o último elemento do índice e atribui esse elemento à constante removido

// pop() remove do final e shift() remove do começo

console.log(nomes, removido);